---
description: Interact with X (Twitter) via the x-twitter skill. Pass a command and flags, e.g. /x-twitter search "claude code"
allowed-tools: Bash(node *), Bash(npm *), Bash(npx *), Bash(ls *)
argument-hint: "<command> [flags]"
---

You are invoking the x-twitter skill. The user's input is: $ARGUMENTS

Read @plugins/x-twitter/SKILL.md for the full command reference, then execute the requested command.
